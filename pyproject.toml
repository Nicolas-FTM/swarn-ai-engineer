[project]
authors = [{name = "Nicolas-FTM", email = "nicolasfelipetm@gmail.com"}]
dependencies = []
name = "Swarn de Agentes"
requires-python = ">= 3.11"
version = "0.1.0"

[build-system]
build-backend = "hatchling.build"
requires = ["hatchling"]

[tool.pixi.workspace]
channels = ["conda-forge"]
platforms = ["win-64"]

[tool.pixi.pypi-dependencies]
example = { path = ".", editable = true }

[tool.pixi.tasks]
