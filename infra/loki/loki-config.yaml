auth_enabled: false

ingester:
  chunk_idle_period: 3m
  chunk_retain_period: 1m
  max_chunk_age: 1h
  chunk_encoding: snappy
  wal:
    dir: /var/loki/wal

limits_config:
  reject_old_samples: true
  reject_old_samples_max_age: 168h
  allow_structured_metadata: false

schema_config:
  configs:
    - from: 2020-10-24
      store: boltdb-shipper
      object_store: filesystem
      schema: v11
      index:
        prefix: index_
        period: 24h

server:
  http_listen_port: 3100
  grpc_listen_port: 9096

storage_config:
  boltdb_shipper:
    active_index_directory: /var/loki/loki/boltdb-shipper-active
    cache_location: /var/loki/loki/boltdb-shipper-cache
  filesystem:
    directory: /var/loki/chunks

common:
  path_prefix: /var/loki
  replication_factor: 1
  storage:
    filesystem:
      chunks_directory: /var/loki/chunks
      rules_directory: /var/loki/rules
  ring:
    kvstore:
      store: inmemory

table_manager:
  retention_deletes_enabled: false
  retention_period: 0s


# TODO: check if it is worth it to use Alertmanager
# ruler:
#   alertmanager_url: http://localhost:9093   # TODO: Just in case if you use AlertManager
